ListDic1 = [
    {'name': 'Leodeymison', 'age': 20},
    {'name': 'Pedro', 'age': 13}
]

for item in ListDic1:
    print('Nome: {}\nIdade: {}\n'.format(item['name'], item['age']))


### ? dict.clear()

### ? dict.copy()

### ? dict.fromkeys()

### ? dict.get(key, default=None)

### ? dict.has_key(key)

### ? dict.items()

### ? dict.keys()

### ? dict.pop()

### ? dict.popitem()

### ? dict.setdefault(key, default=None)

### ? dict.update(dict2)

### ? dict.values()
