
### ? import
# * Importa outro arquivo
import calcs
import os

print('Soma dos valores é: {}'.format(calcs.soma(12, 22)))

# print('Retorna o nome físico do módulo: {}'.format(calcs.__file__))
# print('Retorna o pacote ao qual o módulo pertence: {}'.format(os.__package__))
# print('Retorna a docstring no topo do módulo, se houver: {}'.format(os.__doc__))
# print('Retorna todo o escopo do módulo: {}'.format(os.__dict__))
# print('Retorna o nome do módulo: {}'.format(os.__name__))

"The docstring of mymodule"
def sum(x,y):
   return x+y

if __name__ == "__main__":
    print("sum:",sum(10,20))

### ? Sistema operacional (os)
# * Este módulo fornece uma interface unificada para diversas funções do sistema operacional.

### ? string
# * Este módulo contém uma série de funções para processamento de strings

### ? re
# * Este módulo fornece um conjunto de recursos poderosos de expressões regulares

### ? math
# * Este módulo implementa uma série de operações matemáticas para números de ponto flutuante

### ? cmath
# * Este módulo contém uma série de operações matemáticas para números complexos.

### ? datetime
# * Este módulo fornece funções para lidar com datas e horas dentro de um dia.

### ? gc
# * Este módulo fornece uma interface para o coletor de lixo integrado.

### ? asyncio
# * Este módulo define a funcionalidade necessária para processamento assíncrono

### ? Collections
# * Este módulo fornece tipos de dados avançados de contêiner.

### ? Functools
# * Este módulo possui funções e operações de ordem superior em objetos que podem ser chamados

### ? operator
# * Funções correspondentes aos operadores padrão.

### ? pickle
# * Converta objetos Python em fluxos de bytes e vice-versa.

### ? socket
# * Interface de rede de baixo nível.

### ? sqlite3
# * Uma implementação DB-API 2.0 usando SQLite 3.x.

### ? statistics
# * Funções de estatística matemática

### ? typing
# * Suporte para dicas de tipo

### ? venv
# * Criação de ambientes virtuais.

### ? json
# * Codifique e decodifique o formato JSON.

### ? wsgiref
# * Utilitários WSGI e implementação de referência.

### ? unittest
# * Estrutura de testes unitários para Python.

### ? random
# * Gerar números pseudo-aleatórios


####### ? Funções integradas

### ? abs()

### ? aiter()

### ? all()

### ? anext()

### ? any()

### ? ascii()

### ? bin()

### ? bool()

### ? breakpoint()

### ? bytearray()

### ? bytes()

### ? callable()

### ? chr()

### ? classmethod()

### ? compile()

### ? complex()

### ? delattr()

### ? dict()

### ? dir()

### ? divmod()

### ? enumerate()

### ? eval()

### ? exec()

### ? filter()

### ? float()

### ? format()

### ? frozenset()

### ? getattr()

### ? globals()

### ? hasattr()

### ? hash()

### ? help()

### ? hex()

### ? id()

### ? input()

### ? int()

### ? isinstance()

### ? issubclass()

### ? iter()

### ? len()

### ? list()

### ? locals()

### ? map()

### ? max()

### ? memoryview()

### ? min()

### ? next()

### ? object()

### ? oct()

### ? open()

### ? ord()

### ? pow()

### ? print()

### ? property()

### ? range()

### ? repr()

### ? reversed()

### ? round()

### ? set()

### ? setattr()

### ? slice()

### ? sorted()

### ? staticmethod()

### ? str()

### ? sum()

### ? super()

### ? tuple()

### ? type()

### ? vars()

### ? zip()

### ? __import__()


####### ? Funções matemáticas integradas
### ? abs()

### ? max()

### ? min()

### ? pow()

### ? round()

### ? sum()
